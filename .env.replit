# Environment variables for Replit deployment
PORT=3000
PERFORMANCE_MODE=balanced
CACHE_TTL=1800
LOG_LEVEL=info
LOG_TO_FILE=false
NODE_ENV=production

# Optional: Set to 'fast' for better Replit performance
# PERFORMANCE_MODE=fast

# Optional: Enable file logging (not recommended for Replit)
# LOG_TO_FILE=true
